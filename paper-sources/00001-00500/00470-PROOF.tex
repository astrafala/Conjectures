\documentclass[11pt,a4paper]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb,amsthm}
\usepackage[margin=1in]{geometry}
\usepackage[colorlinks=true,linkcolor=blue,urlcolor=blue]{hyperref}
\theoremstyle{plain}
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{remark}[theorem]{Remark}

\title{The empirical recurrence for OEIS A187301, proved from an exact model}
\author{Adrian Perez Fontelles\\ \small Independent researcher}
\date{13 September 2026}
\begin{document}
\maketitle

\begin{abstract}
OEIS A187301 carries an empirical recurrence of order $3$. It is true, and it is
decidable rather than empirical. The entry's sequence is given exactly by a finite sum over walk shapes of the ways each fits the board, from
which $a$ provably satisfies a MONIC linear recurrence of order $S=14$, derived below and not
assumed. The residual of the conjectured recurrence satisfies the same one, so whether it
annihilates $a$ is settled by a finite exact computation. No transfer matrix is involved: the objects counted are walks, but the count is a finite sum over their shapes and not a walk count in any digraph.
\end{abstract}

\noindent\small 2020 Mathematics Subject Classification. 05A15, 05A19, 11P21.\normalsize

\section{The conjecture}

OEIS A187301 is ``Number of 6-step one space leftwards or up, two space rightwards or down asymmetric rook's tours on an n X n board summed over all starting positions.''. It has offset $1$ and begins
\[
0, 0, 144, 884, 3006, 6520, 11896, 18832,\ \dots
\]
The entry states, as a conjecture contributed by its author and never marked settled:
\begin{quote}
Empirical: a(n) = 788*n\^{}2 - 4736*n + 6256 for n>9. a(n) = 3*a(n-1) - 3*a(n-2) + a(n-3) for n>12.
\end{quote}
As of the ``Last modified'' line on the live entry (Apr 23 2018 08:37:51, revision 10)
this is still recorded as empirical, and nothing on the entry records it as proved.

\section{The count is a sum over a FIXED set of shapes}

The number of steps is fixed at $6$ cells, hence $5$ moves, and it is the BOARD
that grows. So the walks themselves do not depend on $n$ at all. Let $M$ be the $4$
vectors the piece may move by, read from the entry's own words, and call two walks the same
SHAPE when one is a translate of the other. There are exactly $788$ shapes: self-avoiding
sequences of $6$ distinct cells in $\mathbf{Z}^{2}$ with each consecutive difference in
$M$, counted up to translation, which a finite search enumerates once and for all.

A shape occupies a bounding box with sides $(w_1,\dots,w_{2})$, the range of its cells in each
coordinate. Placing it on a board of side $n$ means choosing where that box sits, and the
choices in the coordinates are independent, so the shape occurs in exactly
$\prod_{i}\max(0,\,n-w_i)$ positions and in no others. Distinct pairs (starting cell,
walk) are exactly distinct pairs (shape, placement) --- a walk determines its shape and its
translation, and conversely --- so, with $N_w$ the number of shapes of bounding box $w$,
\[
a(n)\;=\;\sum_{w} N_w \prod_{i=1}^{2}\max\bigl(0,\ n-w_i\bigr).
\]
This is exact for every $n$, not asymptotic, and it is not a walk count in any digraph: there is
no transfer matrix here and no window.

\section{The bound}

Write $w^\ast$ for the largest bounding-box side over all $788$ shapes. Once $n\ge w^\ast$
every factor above is nonnegative, so the truncation does nothing and
$a(n)=\sum_w N_w\prod_i(n-w_i)$, a polynomial in $n$ of degree $2$. Hence $(z-1)^{3}$
annihilates $a$ from $n=10$ onwards, and the annihilator in force on the whole sequence is
$z^{11}(z-1)^{3}$, monic of order $S=14$. Both numbers are read off the enumerated
shapes; neither is assumed. The model was then asked for terms beyond those the entry publishes
and reproduced the published ones exactly.

\section{The proof}

\begin{lemma}\label{lem:crit}
Let $A(z)=z^S-\sum_{j<S}\alpha_jz^{j}$ be the monic annihilator derived above and
$q(z)=z^{3}-\sum_ic_iz^{3-i}$ the characteristic polynomial of the conjectured
recurrence. The residual $r(n)=a(n)-\sum_ic_ia(n-i)$ is a linear combination of shifts of $a$,
so $A$ annihilates $r$ as well. Since $A(0)\ne0$ the recurrence it gives runs in both
directions, and $S$ consecutive zeros of $r$ force $r$ to vanish at every later index.
\end{lemma}

\begin{proof}
$A$ annihilates $a$, hence every shift of $a$, hence any linear combination of shifts; that is
$r$. A monic recurrence with nonzero constant term determines each term from the $S$ before it
and each from the $S$ after it, so a run of $S$ zeros propagates.
\end{proof}

\begin{theorem}
\[
a(n)\;=\;3\,a(n-1) - 3\,a(n-2) + a(n-3)
\]
for every $n>12$.
\end{theorem}

\begin{proof}
The terms $a(0),\dots$ were computed exactly in integer arithmetic from the model, extended by
$A$ where the model itself was not evaluated, and $r(n)$ formed for each index. Those integers
vanish from $n=12+1$ onwards, and the run exceeds $S+3$, which by
Lemma~\ref{lem:crit} settles every larger index at once. The bound is exact: at $n=12$ the recurrence fails on the entry's own published terms, so no larger range holds.
\end{proof}

\section{Verification}

Three checks, each independent of the algebra above.

First, the model reproduces all $34$ terms the entry publishes, exactly, in integer
arithmetic. This is what ties the model to the entry: it is built by reading the entry's
English, and a misreading gives different counts.

Second, the conjectured recurrence was evaluated directly on those published terms, with no
model involved, and holds wherever the proved range and the data overlap.

Third, the derived annihilator $A$ was itself tested on terms it had not been given: the model
was evaluated beyond the $S$ values that determine it and $A$ reproduced them. A bound that is
too small fails this test, which is why it is run.

\begin{thebibliography}{9}
\bibitem{oeis} The OEIS Foundation, \emph{The On-Line Encyclopedia of Integer
Sequences}, \url{https://oeis.org}, 2026. Sequence A187301.
\bibitem{beckrobins} M.~Beck and S.~Robins, \emph{Computing the Continuous Discretely},
2nd ed., Springer, 2015. (Ehrhart quasi-polynomials and their periods.)
\bibitem{stanley} R.~P.~Stanley, \emph{Enumerative Combinatorics}, Volume 1, 2nd ed.,
Cambridge University Press, 2011.
\bibitem{ds} F.~Diamond and J.~Shurman, \emph{A First Course in Modular Forms},
Springer GTM 228, 2005.
\end{thebibliography}

\end{document}
