*.log

# The delivery archives are gone: the papers live in papers/, one folder per hardness band,
# and that is the thing to link people to. makezips.py still works if an archive is ever
# wanted, but nothing builds one by default any more.
conjecture-papers-*.zip

# The `was`-keyed master. It is the same 8887 PDFs as papers/ under different names, so
# storing it doubles the clone for nothing; restore_master.py rebuilds it exactly from
# papers/ and rank-map.json, and rank.py needs it on disk before it can run.
papers-old-numbering/

__pycache__/
